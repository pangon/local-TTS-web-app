# OS
.DS_Store
Thumbs.db

# Editors
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Environment and secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# Test
.pytest_cache/

# Runtime data (SQLite DB, audio files, model cache)
3-code/backend/data/

# Build outputs (uncomment what applies to your stack)
# node_modules/
# dist/
# target/
# build/

# Root-level scratch tests only — anchored with a leading slash so component
# test suites (e.g. 3-code/backend/tests/) are NOT ignored.
/tests/

# User-provided local default reference-voice clip (e.g. wavs/default.mp3) used
# by reference-only adapters (CosyVoice 3) as a temporary default until voice
# selection lands (Phase 6). Local asset, never committed.
/wavs/
